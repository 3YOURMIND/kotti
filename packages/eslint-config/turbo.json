{
	"$schema": "https://turbo.build/schema.v1.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"inputs": ["scripts/**", "source/**", "tsconfig.*"],
			"outputMode": "new-only",
			"outputs": ["dist/**"]
		}
	}
}
