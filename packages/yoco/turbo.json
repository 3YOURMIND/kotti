{
	"$schema": "https://turbo.build/schema.v1.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"inputs": ["scripts/**", "source/**", "svg/**", "tsconfig.*"],
			"outputMode": "new-only",
			"outputs": ["dist/**", "fonts/**"]
		}
	}
}
