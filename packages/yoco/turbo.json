{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"pipeline": {
		"build": {
			"inputs": ["scripts/**", "source/**", "svg/**", "tsconfig.*"],
			"outputMode": "new-only",
			"outputs": ["dist/**", "fonts/**"]
		}
	}
}
