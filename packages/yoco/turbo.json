{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["scripts/**", "source/**", "svg/**", "tsconfig.*"],
			"outputLogs": "new-only",
			"outputs": ["dist/**", "fonts/**"]
		}
	}
}
