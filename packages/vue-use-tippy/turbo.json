{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["scripts/**", "source/**", "tsconfig.*"],
			"outputLogs": "new-only",
			"outputs": ["dist/**"]
		}
	}
}
